# Палисад — текущие задачи

**Обновлено:** 2026-06-12

Смотри, как сейчас будем работать. В этом файле General Tasks изложены таски, которые надо реализовать. Берешь одну из этих таск, в папке tasks, создаешь файл MD с названием этой таски. Например, DarkThem или LicenseTestCases или что-то еще, в зависимости от того, какая таска. Туда, в этот MD файл, вначале пишешь, чего хотим достичь. План по этой таске и после того как мы это реализуем, я этот план согласую, приступаешь к реализации. По итогам реализации пишешь, что было сделано по этой таске, какого результата удалось добиться.

## Контекст

**Палисад** — offline-first PWA для учёта питомников растений (Vue 3 + PrimeVue / Node.js + Express + PostgreSQL).
Полное описание продукта — [`general.md`](../general.md), обзоры — [`general-overview.md`](general-overview.md),
[`backend-overview.md`](backend-overview.md), [`frontend-overview.md`](frontend-overview.md),
план v2 — [`v2-roadmap-proposal.md`](v2-roadmap-proposal.md).

## Текущее состояние

**MVP (v1) реализован end-to-end:**

- Backend: модули 0–13 (инициализация, БД/миграции, auth, питомник, RBAC, сотрудники,
  подписки, локации, справочники, реестр растений, операции/фото, движения, QR/этикетки,
  лента активности) — код присутствует по всем слоям (routes → controllers → services → repositories).
- Frontend: модули 0–12 (Dexie offline store, auth, питомник/подписка, сотрудники, локации,
  справочники, реестр, операции, движения, сканер/этикетки, лента, офлайн-синхронизация) —
  все страницы, сторы и offline-инфраструктура на месте.
- Последние работы (по git): миграция на PrimeVue 4 и Tailwind, GBIF-поиск, переработка диалогов, документация.

**Разрыв закрыт (2026-06-13):** критерии приёмки модулей 2–13 сверены, статус «Реализовано»
проставлен во всех `MODULE_*.md`. Проверка: `backend/scripts/acceptance-check.mjs` (80/81 PASS)
+ 130 интеграционных тестов (`backend/tests/`, покрытие строк/функций 93.8%/98.1%).

## Задачи

### 1. Стабилизация MVP (перед стартом v2) — ✅ выполнено (2026-06-13)

- [x] Прогнать критерии приёмки модулей 2–13 (таблицы «Критерии приёмки» в каждом `MODULE_*.md`),
      проставить статусы «Реализовано» в доках по факту проверки. _(80/81 PASS, см. [`MvpStabilization.md`](MvpStabilization.md))_
- [x] Сквозной smoke-сценарий: регистрация → питомник → сотрудник → растение (GBIF-вид) →
      операция с фото → движение → этикетка PDF → лента активности.
- [x] Проверить офлайн-цикл: офлайн-операция/движение → `sync_queue` → синхронизация → конфликт
      («сервер побеждает» + уведомление), `retries >= 3 → failed`. _(код-ревью; server-wins — находка, см. ниже)_
- [x] Заполнить корневой `README.md` (сейчас одна строка): запуск через `docker-compose`,
      миграции/сиды, dev-команды фронта и бэка.
- [x] проверить достаточность бэкенда для запланированных функций. _(аудит 8 этапов v2, см. [`BackendHardening.md`](BackendHardening.md))_
- [x] проверить запросы в бд на коректность и осутвие запросов N + 1. _(2 фикса: SQL-пагинация растений, N+1 в этикетках)_
- [x] добавить интеграционные тесты для бэка с покрытием >90. _(130 тестов; строки/функции 93.8%/98.1%, ветки 82.7%)_


### 2. v2 · Этап 1 — Несколько питомников на одном аккаунте

Первый этап v2 по роадмапу (минимально инвазивный, схема уже готова: `nurseries.account_id` 1:N).

- [ ] Backend: снять предположение «один питомник на аккаунт» в бизнес-логике
      (nursery.service, auth-flow после логина).
- [ ] Backend: подключить `plans.nursery_limit` к `checkLimit` при создании питомника.
- [x] Решение по ролям: роли сотрудников остаются per-nursery (рекомендация роадмапа) — зафиксировано,
      см. [`MultiNurseryRolesDecision.md`](MultiNurseryRolesDecision.md).
- [ ] Frontend: переключатель активного питомника (шапка/настройки), «активный питомник»
      в `nursery.store`, `nurseryId` во всех запросах (роуты `/nurseries/:nurseryId/...` уже готовы).
- [ ] Офлайн: ограничить офлайн-кэш одним активным питомником за раз (простой вариант из роадмапа);
      при переключении — пере-синхронизация.

### 3. Дальше по роадмапу v2 (очерёдность)

- [x] **2. Уведомления (инфраструктура, in-app)** — реализовано, см.
      [`Notifications.md`](Notifications.md) (таблица `notifications`, хелпер `notify`,
      REST + колокольчик с пуллингом, продюсер смены роли; 148/148 тестов).
- [ ] 3. Производственные стадии → 4. Себестоимость → 5. Отчёты и аналитика →
      6. Задачи и планирование → 7. Склад и зимовка → 8. Продажи и CRM.

> ⚠️ Себестоимость (4) и Продажи/CRM (8) — зоны повышенного риска, требуют
> предварительного обсуждения с заказчиком (см. роадмап). Перед ними — пауза на согласование.

## Принципы работы (из роадмапа)

- Каждый этап закрывается end-to-end (схема → backend → frontend → базовый тест) и отгружается отдельно.
- Новые поля — `NULL`-able или с дефолтами; сохранять `updated_at`, `deleted_at`, CHECK, partial-индексы.
- Новые офлайн-сущности — продумывать предзагрузку и мутации через `sync_queue`; агрегации (себестоимость, отчёты) — только онлайн.
- Себестоимость и CRM — зоны повышенного риска, требуют предварительного обсуждения с заказчиком.

claude --resume bb2758ca-1a11-4226-a03d-c69dee81c61c